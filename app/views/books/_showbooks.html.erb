<div class="container">
	<div class="row">
	<div class="col-md-12">
 <% @user.books.each do |book| %>
    <tr>
      <td><p><%= book.title %> Pages <%= book.pages %><p>
    <%= link_to 'Destroy', user_book_path(book.id), :method => :delete, :onclick => "return confirm('Are you sure you want to delete this post?')" %>
      </td>
    </tr>
  <% end %>

	<p class="text-primary">
		<%= link_to "Add", new_user_book_path(@user.id), :class=>'btn btn-primary btn-xs'%></p>
		</div>
</div>
</div>